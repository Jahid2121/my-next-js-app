import React from 'react';

export const metadata = {
    title: "About page",
    description: "This is about page"
}

const AboutPage = () => {
    return (
        <div>
            <h2>About Page with header and footer</h2>
        </div>
    );
};

export default AboutPage;